import { html } from 'lit';
import { Meta, Canvas, Story, Preview } from '@storybook/addon-docs';

import { extractPx } from '../../../components/base/outline-element/utils/utils';
import tailwindThemeConfig from '../../../resolved-tailwind-config';

<Meta
  title="Design Tokens/Spacing"
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: {
        hidden: true,
      },
    },
  }}
/>

# Spacing values

<Story name="Spacing">
  {() => {
    const spacings = Object.entries(tailwindThemeConfig.spacing).map(
      ([name, value]) => html`
        <tr>
          <td class="py-2 text-center border-b border-demo-lightGray">
            ${name}
          </td>
          <td class="py-2 text-center border-b border-demo-lightGray">
            ${value}
          </td>
          <td class="py-2 text-center border-b border-demo-lightGray">
            ${extractPx(value)}
          </td>
          <td class="py-2 text-center border-b border-demo-lightGray">
            <div class="rounded h-6 bg-demo-darkGray w-${name}"></div>
          </td>
        </tr>
      `
    );
    return html`
      <div class="prose">
        <h2 class="group flex whitespace-pre-wrap mb-0" id="overview">
          <span class="sr-only">Overview</span>
        </h2>
        <p>
          The consistent, finite set of spacings used throughout components.
        </p>
        <div class="mt-10"></div>
      </div>
      <table
        class="w-full rounded-sm table-auto shadow-lg bg-demo-darkGray overflow-hidden"
      >
        <thead class="text-xl font-display">
          <tr class="border-neutral-gray-100 bg-demo-lightGray">
            <th scope="col" class="p-3 font-semibold text-neutral-black">
              Name
            </th>
            <th scope="col" class="p-3 font-semibold text-neutral-black">
              Size
            </th>
            <th scope="col" class="p-3 font-semibold text-neutral-black">
              Pixels
            </th>
            <th scope="col" class="p-3 font-semibold text-neutral-black">
              Example
            </th>
          </tr>
        </thead>
        <tbody class="font-body bg-demo-white">
          ${spacings}
        </tbody>
      </table>
    `;
  }}
</Story>

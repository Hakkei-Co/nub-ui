import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { html } from 'lit';
import { NubSwatch } from './nub-swatch';
import { swatchSets } from '../nub-swatch-set/nub-swatch-set';

<Meta
  title="Utilities/Swatch"
  component="nub-swatch"
  argTypes={{
    set: {
      name: 'set',
      options: swatchSets,
      control: { type: 'select' },
      description: 'The color set.',
    },
    color: {
      name: 'color',
      description: 'The color.',
    },
  }}
/>

export const Template = ({ set, color }) => html`
  <nub-swatch set="${set}" color="${color}"> </nub-swatch>
`;

## Primary color

<Canvas>
  <Story
    name="Nubs Primary"
    args={{
      set: 'nubs',
      color: 'primary',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Secondary color

<Canvas>
  <Story
    name="Nubs Secondary"
    args={{
      set: 'nubs',
      color: 'secondary',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
